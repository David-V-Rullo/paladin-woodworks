import Link from 'next/link';
export const projects=[{id:'watch-box',number:'01',name:'The watch box',category:'SMALL BOXES',status:'Planner prototype',description:'A compact case with configurable compartments, corner joinery, and a captured bottom.',href:'/planner',link:'Open watch-box planner'},{id:'whiskey-cabinet',number:'02',name:'The whiskey cabinet',category:'CABINETRY',status:'Planned project',description:'A display cabinet for bottles and glassware, with the door and joinery at the center of the design.'},{id:'storage-box',number:'03',name:'The storage box',category:'SMALL BOXES',status:'Planned project',description:'A humidor-style box exploring fitted compartments, a removable tray, and thoughtful lid details.'}];
export function PhotoPlaceholder({label,number='PW',className=''}:{label:string;number?:string;className?:string}){return <div className={`photo-placeholder ${className}`} role="img" aria-label={`${label}: photograph placeholder`}><span className="placeholder-index">{number}</span><span className="placeholder-caption"><span>{label}</span><small>PHOTOGRAPH TO FOLLOW</small></span></div>}
export function ProjectCards(){return <div className="project-grid">{projects.map(p=><article className="project-card" key={p.id} id={p.id}><PhotoPlaceholder label={p.name} number={p.number}/><div className="project-copy"><div className="project-meta"><span>{p.category}</span><span>{p.status}</span></div><h2>{p.name}</h2><p>{p.description}</p>{p.href?<Link className="text-link" href={p.href}>{p.link} <span aria-hidden="true">↗</span></Link>:<p className="project-note">Project photographs and build details to follow.</p>}</div></article>)}</div>}
export function SiteFooter(){return <footer className="public-footer"><Link href="/">PALADIN WOODWORKS</Link><span>Woodworking, from idea to assembly.</span><Link href="/planner">Open the planner</Link></footer>}
